# Notes of what I did

- I've created CLAUDE.md file using '/init' command in Claude. I've cleaned unnecessary line which are obvious from the code. And also I've added some commands to run tests. Conventions and Architecture sections did not need any modifications since they were spot on after the '/init' command.
- I've added permissions to the project. I was using '/permissions' command in Claude. As the result I've got .claude/settings.json file
- I've also tested the project by running all the commands from the Commands section of the CLAUDE.md file
- The README.md was a bit confused so I've committed .claude/settings.json and CLAUDE.md directly to the main brunch. NOTE: You should rewrite section "4. Add permission rules" and remove this line "Commit `.claude/settings.json` so the rules are shared."
